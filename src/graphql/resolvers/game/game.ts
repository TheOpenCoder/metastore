// import * as _ from 'lodash';

// import { Resolvers } from '../../../types/codegen.types';
// import { ResolverContext } from '../../../types/context';

// const resolvers: Resolvers = {
//   Game: {
//     title: async (parent, args, {}: ResolverContext) => {
//       return 'Game title';
//     },

//     __isTypeOf: (obj) => {
//       return !_.isNil(obj.title);
//     },
//   },
// };

// export default resolvers;
